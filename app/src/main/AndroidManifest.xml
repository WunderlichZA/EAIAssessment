<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.proedva.androidassessmenteai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".views.EAIAndroidApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAssessmentEAI"
        tools:targetApi="31">
        <activity
            android:name=".views.HomeBottomNavigation"
            android:exported="false"
            android:label="@string/title_activity_home_bottom_navigation" />
        <!--<activity
            android:name=".Home"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.AndroidAssessmentEAI.NoActionBar" /> -->
        <activity
            android:name=".views.MainActivity"
            android:exported="true" />
        <activity
            android:name=".feature_login.views.CustomerLoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidAssessmentEAI.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>